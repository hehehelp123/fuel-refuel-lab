# -*- coding: utf-8 -*-
"""Уточняет таблицу улучшений: варианты независимые, а не накопительные."""
import io
import sys

README = "C:/Users/79101/Downloads/Лаба_заправки/README.md"

old = """| вариант | событийный F1 |
|---|---|
| бейзлайн ноутбука (отбор эпохи по точечному F1) | 0.786 |
| отбор эпохи по событийному F1 | **0.959** |
| то же + `ign`, `speed`, `n_msg` в признаках | **0.971** |
| то же + нормализация входов | **0.992** |
| окно 11 интервалов вместо 7 | 0.962 |
| двунаправленная LSTM | 0.953 |
| постобработка: блоки короче двух интервалов отбрасываются | 0.821 |"""

new = """| вариант | событийный F1 |
|---|---|
| бейзлайн ноутбука: эпоха выбирается по точечному F1 | 0.786 |
| эпоха выбирается по событийному F1 | **0.959** |
| и добавлена нормализация входов | **0.992** |
| и добавлены признаки `ign`, `speed`, `n_msg` | **0.971** |
| и окно 11 интервалов вместо 7 | 0.962 |
| и двунаправленная LSTM | 0.953 |
| и всё перечисленное сразу | 0.930 |
| и постобработка: блоки короче двух интервалов отбрасываются | 0.821 |

Все строки, кроме первой, отбирают эпоху по событийному F1 — то есть каждая
следующая строка это **одно изменение поверх 0.959**, а не накопление предыдущих.
Исключение — строка «всё перечисленное сразу»."""

s = io.open(README, encoding="utf-8").read()
if old not in s:
    sys.exit("таблица не найдена")
io.open(README, "w", encoding="utf-8").write(s.replace(old, new, 1))
print("таблица уточнена")
