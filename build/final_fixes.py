# -*- coding: utf-8 -*-
"""Последние правки: устаревшее число и уточнение разбиения."""
import io
import sys

NL = chr(10)
README = "C:/Users/79101/Downloads/Лаба_заправки/README.md"
SLIDES = "C:/Users/79101/Downloads/Лаба_заправки/build/slides_content.py"

readme_pairs = [
    ("""Основной запас для улучшения виден здесь же: модель путает заправку с включённым двигателем
с заправкой с выключенным двигателем (72 интервала). Если студент объединит классы 1 и 2 в один
«была заправка», прирост он получит почти сразу. Засчитывать это стоит с оговоркой,
что при таком объединении меняется постановка задачи.""",

     """Основной запас для улучшения виден здесь же: модель путает заправку с включённым
двигателем с заправкой с выключенным двигателем — 91 интервал из 359 уехал в класс 2.
Объединение классов 1 и 2 даёт большой прирост, но это ловушка: разбор в разделе
«Что реально даёт прирост»."""),

    ("""Эпоха выбирается по отложенной валидации из девяти обучающих машин,
тест участвует только в итоговом замере.""",

     """Из 45 обучающих машин девять отложены под валидацию — на ней выбирается эпоха.
Модель обучается на оставшихся 36, тест участвует только в итоговом замере."""),
]

slide_pairs = [
    ('''             "45 в обучении, 5 в тесте. Классы: 0 — ничего, 1 — заправка "''',
     '''             "36 обучение, 9 валидация, 5 тест. Классы: 0 — ничего, 1 — заправка "'''),
]


def patch(path, pairs, label):
    s = io.open(path, encoding="utf-8").read()
    for old, new in pairs:
        if old not in s:
            sys.exit("[" + label + "] НЕ НАЙДЕНО:" + NL + old[:160])
        s = s.replace(old, new, 1)
    io.open(path, "w", encoding="utf-8").write(s)
    print(label + ": исправлено " + str(len(pairs)))


patch(README, readme_pairs, "README")
patch(SLIDES, slide_pairs, "слайды")
