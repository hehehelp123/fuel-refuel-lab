# -*- coding: utf-8 -*-
"""Презентация лабораторной на шаблоне ИТМО.

За основу берётся мастер, макеты и тема из дипломной презентации: фирменный
красный, чёрно-серая гамма, 16:9 на 10 дюймов. Слайды шаблона удаляются,
новые собираются на его же макетах.
"""
import copy
import os

from PIL import Image
from pptx import Presentation
from pptx.util import Inches, Pt

TEMPLATE = "C:/Users/79101/Desktop/Диплом/Презентация_Халеев_М_Д.pptx"
IMG = "C:/Users/79101/Downloads/Лаба_заправки/slides_img"
OUT = "C:/Users/79101/Downloads/Лаба_заправки/Лаба_заправки.pptx"

L_TITLE, L_BODY, L_FINAL = 0, 9, 13     # индексы макетов в мастере
TITLE_PT = 24

prs = Presentation(TEMPLATE)
SW = prs.slide_width / 914400

# --- запоминаем оформление номера слайда, чтобы не выдумывать своё
page_no_proto = None
for slide in prs.slides:
    for sh in slide.shapes:
        if not sh.is_placeholder and sh.has_text_frame and sh.text_frame.text.strip().isdigit():
            page_no_proto = copy.deepcopy(sh._element)
            break
    if page_no_proto is not None:
        break

# --- чистим слайды шаблона, мастер и тема остаются
sld_id_lst = prs.slides._sldIdLst
for sld_id in list(sld_id_lst):
    prs.part.drop_rel(sld_id.rId)
    sld_id_lst.remove(sld_id)


def body_of(slide):
    for ph in slide.placeholders:
        if ph.placeholder_format.idx != 0:
            return ph
    return None


def fill(tf, items, size=None):
    """items: строки или пары (текст, уровень)."""
    tf.clear()
    for i, item in enumerate(items):
        text, level = item if isinstance(item, tuple) else (item, 0)
        para = tf.paragraphs[0] if i == 0 else tf.add_paragraph()
        para.level = level
        run = para.add_run()
        run.text = text
        if size:
            run.font.size = Pt(size)


def add(layout_idx, title, items, size=None, body_box=None,
        picture=None, pic_box=None):
    slide = prs.slides.add_slide(prs.slide_masters[0].slide_layouts[layout_idx])

    if slide.shapes.title is not None:
        tf = slide.shapes.title.text_frame
        tf.clear()
        run = tf.paragraphs[0].add_run()
        run.text = title
        run.font.size = Pt(TITLE_PT)

    body = body_of(slide)
    if body is not None:
        if items:
            fill(body.text_frame, items, size)
        else:
            body._element.getparent().remove(body._element)
            body = None
        if body is not None and body_box:
            body.left, body.top = Inches(body_box[0]), Inches(body_box[1])
            body.width, body.height = Inches(body_box[2]), Inches(body_box[3])

    if picture:
        path = os.path.join(IMG, picture)
        iw, ih = Image.open(path).size
        x, y, w = pic_box
        slide.shapes.add_picture(path, Inches(x), Inches(y),
                                 width=Inches(w), height=Inches(w * ih / iw))

    if page_no_proto is not None:
        el = copy.deepcopy(page_no_proto)
        slide.shapes._spTree.append(el)
        for sh in slide.shapes:
            if sh._element is el and sh.has_text_frame:
                for para in sh.text_frame.paragraphs:
                    for run in para.runs:
                        run.text = str(len(prs.slides._sldIdLst))
                    break
    return slide


# ------------------------------------------------------------------ слайды
add(L_TITLE, "Детекция заправок по датчику уровня топлива", [
    "Лабораторная работа",
    "Данные автопарка: 50 машин, 6.1 млн сообщений телематики, июль — декабрь 2025",
])

add(L_BODY, "Задача", [
    "На машине установлен датчик уровня топлива, телематический блок передаёт "
    "показания раз в несколько секунд",
    "По этому временному ряду нужно определять моменты заправок",
    "Это основа топливного учёта в автопарке: иначе баланс топлива сводят "
    "вручную по чекам",
    "В данных за полгода размечено 1249 заправок на 50 машинах",
])

add(L_BODY, "Что приходит с машины", [
    "Уровень топлива в условных единицах датчика, состояние зажигания, "
    "скорость, время",
    "Сообщения приходят неравномерно: от одного в секунду на ходу до одного "
    "в несколько минут на стоянке",
    "Тарировка: у каждой машины своя таблица «показание — литры», перевод "
    "выполняется линейной интерполяцией между её узлами",
    "Без тарировки одна и та же заправка на 40 литров даёт на разных машинах "
    "разную амплитуду, и общая модель работать не может",
])

add(L_BODY, "Почему порог на приросте работает плохо", [
    "Датчик запитан от зажигания: при заглушенном двигателе показаний нет, "
    "а заправка — это чаще всего стоянка",
    "При работающем двигателе уровень колеблется на несколько литров "
    "из-за разгонов и уклонов",
    "Низкий порог срабатывает на этих колебаниях, высокий пропускает "
    "небольшие заправки",
    "На наших данных лучший порог даёт событийный F1 = 0.37",
])

add(L_BODY, "Зажигание делит задачу на две", [
    "Двигатель заглушен: роста уровня в данных нет совсем. Известен уровень "
    "до остановки, затем пропуск, затем уровень после запуска. 711 событий",
    "Двигатель работает: уровень растёт монотонно 10–20 минут, рост виден "
    "напрямую. 538 событий",
    "Представления данных разные, поэтому и моделей две",
    "В лабораторной работаем со вторым случаем",
])

add(L_BODY, "Заправка при работающем двигателе", [
    "Один интервал — 5 минут",
    "Заправка занимает 3–4 интервала, уровень растёт с 17 до 75 литров",
    "Серым отмечена разметка оператора, её границы поставлены приблизительно",
], size=14, body_box=(6.1, 1.35, 3.4, 3.7),
    picture="s_event.png", pic_box=(0.5, 1.9, 5.4))

add(L_BODY, "Датасет лабораторной", [
    "Файл fuel_5min.csv, 85 228 строк; одна строка — 5 минут работы одной машины",
    "Столбцы: vehicle, ts, fuel, ign, speed, n_msg, label, split",
    "Классы: 0 — ничего, 1 — заправка на ходу, 2 — заправка на стоянке",
    "Разбиение по машинам: 45 в обучении, 5 в тесте",
    "Случайное разбиение по точкам завысило бы метрику: окна одного события "
    "попали бы и в обучение, и в тест",
], size=15)

add(L_BODY, "Дисбаланс классов", [
    "Целевой класс занимает 1.8% интервалов",
    "Модель, всегда предсказывающая ноль, даёт 98% accuracy и не находит "
    "ни одной заправки",
    "В функции потерь используются веса классов: корень из отношения частот, "
    "ограниченный сверху",
], size=14, body_box=(5.8, 1.35, 3.7, 3.7),
    picture="s_balance.png", pic_box=(0.5, 1.7, 5.0))

add(L_BODY, "Признаки и окна", [
    "Признаки интервала: уровень топлива и четыре разности с уровнем "
    "на 1, 2, 3 и 4 интервала назад",
    "Разности разной глубины: короткая замечает резкий скачок, длинная — "
    "медленный рост, растянутый на 20 минут",
    "Окно — 7 интервалов подряд, то есть 35 минут",
    "Класс окна равен классу его центрального интервала",
    "Получается 74 993 окна в обучении и 9 935 в тесте",
], size=15)

add(L_BODY, "Модель и обучение", [
    "Двухслойная LSTM, 64 скрытых нейрона, dropout 0.2, линейный слой "
    "на три класса",
    "51 651 параметр",
    "Adam, скорость обучения 0.001, размер батча 512, 30 эпох",
    "Обучение на процессоре занимает 45 секунд",
    "Та же архитектура используется в рабочем сервисе",
], size=15)

add(L_BODY, "Почему точечных метрик недостаточно", [
    "Оператору нужен ответ «машина заправилась в такое-то время», "
    "а не «этот пятиминутный интервал относится к заправке»",
    "Модель, угадавшая 3 интервала из 5, по точечной метрике выглядит средне, "
    "но событие она нашла",
    "Одиночное срабатывание посреди стоянки по точечной метрике почти незаметно, "
    "хотя это ложная тревога",
])

add(L_BODY, "Событийная метрика", [
    "События — непрерывные блоки класса 1 внутри одной машины",
    "Предсказание засчитывается, если пересекается с эталонным событием "
    "с допуском ±5 минут",
    "Допуск в 5 минут — это один интервал, поэтому соседний интервал "
    "не считается ошибкой",
    "Предсказания, попавшие на класс 2, не штрафуются: разметке там "
    "нельзя доверять",
    "Из TP, FP и FN считаются precision, recall и F1",
], size=15)

add(L_BODY, "Результаты", [
    "Точечный F1 по классу 1: 0.750",
    "Событийные: precision 1.000, recall 0.744, F1 0.853",
    "Найдено 96 событий, пропущено 33",
    "72 интервала заправки на ходу отнесены к классу 2 — здесь основной "
    "запас для улучшения",
], size=14, body_box=(4.1, 1.45, 5.4, 3.5),
    picture="s_confusion.png", pic_box=(0.5, 1.45, 3.2))

add(L_BODY, "Сравнение с пороговым правилом", [
    "Порог перебран по сетке от 1 до 20 литров, лучший результат 0.373",
    "При любом пороге recall высокий, а precision около 0.22",
    "Правило находит заправки вместе с большим числом колебаний уровня",
    "Модель отличает рост от шума по форме окна, а не по одному числу",
], size=14, body_box=(5.5, 1.35, 4.0, 3.7),
    picture="s_compare.png", pic_box=(0.5, 1.7, 4.7))

add(L_BODY, "Задание", [
    "Увеличить событийный F1 на тестовых машинах; планка «хорошо» — выше 0.88",
    "Что можно попробовать: добавить признаки (ign, speed, n_msg, время суток), "
    "нормализовать входы, изменить длину окна и веса классов, взять "
    "двунаправленную LSTM или свёртку, сгладить предсказания, подобрать порог "
    "вероятности вместо argmax, изменить размер интервала",
    "Разбиение split не менять, на тестовых машинах не обучаться",
    "Гиперпараметры подбирать на обучающей выборке",
    "В отчёте привести таблицу «что изменил — какой получился F1»",
], size=15)

add(L_FINAL, "Вопрос со звёздочкой", [
    "Вторая модель, для заправок при заглушенном двигателе, даёт на тесте "
    "accuracy 100% и F1 = 1.0 по целевому классу",
    "Метка там ставится по правилу: блок ровно из трёх точек, в середине "
    "уровень равен нулю, слева и справа уровни стабильны и различаются",
    "Признаки модели — уровень и его разности",
    "Почему такой результат ничего не говорит о качестве детекции? "
    "Что нужно изменить, чтобы число стало осмысленным?",
])

prs.save(OUT)
print(f"сохранено: {OUT}")
print(f"слайдов: {len(prs.slides.__iter__.__self__._sldIdLst)}")
