# -*- coding: utf-8 -*-
"""Переписывает вопрос со звёздочкой: вместо «правила разметки» — измеренные числа."""
import io
import sys

NL = chr(10)
NB = "C:/Users/79101/Downloads/Лаба_заправки/build/build_notebook.py"
README = "C:/Users/79101/Downloads/Лаба_заправки/README.md"
SLIDES = "C:/Users/79101/Downloads/Лаба_заправки/build/slides_content.py"
FIG = "C:/Users/79101/Downloads/Лаба_заправки/build/analyze_off_task.py"

nb_pairs = [
    ("""### Отдельный вопрос со звёздочкой

В том же проекте есть вторая модель — для заправок с выключенным двигателем.
На тесте она даёт **accuracy 100% и F1 = 1.0** по целевому классу.

Разметка там ставится так: блок из ровно трёх точек, где в середине уровень равен нулю,
а слева и справа уровни стабильны и различаются. Признаки — уровень и его разности.

Вопрос: почему стопроцентный результат в этой постановке ничего не говорит о качестве
детекции? Что нужно поменять, чтобы число стало осмысленным?""",

     """### Отдельный вопрос со звёздочкой

В том же проекте есть вторая модель — для заправок с выключенным двигателем.
На тесте она даёт **accuracy 100% и F1 = 1.0** по целевому классу.

Её задача устроена так. Когда двигатель заглушен, датчик молчит, и в данных
остаётся провал. Всего таких провалов **11 758**, из них эксперт отметил заправками
**711**. Модель должна отличить одни от других по уровню до провала и после.

Возьмите разницу «уровень после минус уровень до» и поставьте на неё порог.
У заправок эта разница в среднем 36 литров, у обычных стоянок — ноль.

Вопросы:

1. Какой F1 даёт простой порог на этой разнице? (у нас вышло 0.968 при пороге 15 л)
2. Сколько тогда добавляет нейросеть поверх одного вычитания?
3. Что означает её «стопроцентная точность» и что нужно поменять, чтобы
   число стало осмысленным?"""),
]

readme_pairs = [
    ("""### Вопрос со звёздочкой

В проекте есть вторая модель — для заправок с выключенным двигателем. На тесте она
даёт accuracy 100% и F1 = 1.0. Студентов просят объяснить, почему это число ничего
не говорит о качестве детекции.

**Ответ для преподавателя.** Метка там ставится по правилу: блок ровно из трёх точек,
в середине уровень равен нулю, слева и справа уровни стабильны и различаются.
Признаки модели — уровень и его разности, то есть ровно те величины, из которых
это правило и построено. Классификатор не учит физику заправки, а восстанавливает
детерминированное правило разметки. Показатель, что это так: идеальное разделение
класса с долей 1 к 1400 достигается на второй эпохе.

Чтобы число стало осмысленным, нужны метки, полученные независимо от самого ряда
уровня: чеки АЗС или транзакции топливных карт.""",

     """### Вопрос со звёздочкой

В проекте есть вторая модель — для заправок с выключенным двигателем. На тесте она
даёт accuracy 100% и F1 = 1.0. Студентов просят объяснить, почему это число ничего
не говорит о качестве детекции.

**Ответ для преподавателя.** Разметка здесь экспертная, никакой подмены меток нет.
Дело в том, что сама подзадача элементарная.

Когда двигатель заглушен, датчик молчит, и в данных остаётся провал. Всего провалов
11 758, из них эксперт отметил заправками 711. Отличить одни от других можно одним
вычитанием: у заправок уровень после провала выше уровня до в среднем на 36 литров,
у обычных стоянок разница нулевая. Замер: **порог 15 литров даёт F1 = 0.968** —
без всякой сети.

То есть сеть добавляет к простому вычитанию около трёх процентов. Оставшееся она
добирает потому, что видит окно из пяти точек, а условие, по которому событие
попадает в этот класс (три точки, по краям уровни стабильны), считается по тому же
окну. Отсюда ровная единица.

Вывод, к которому стоит подвести: 100% означают «подзадача решается одной формулой,
и сеть эту формулу выучила», а не «модель отлично находит заправки». Для сравнения,
вторая ветка, где различить рост от плеска действительно трудно, даёт 0.786.

Что поменять: мерить по независимому источнику (чеки АЗС, топливные карты) и всегда
показывать рядом результат простого порога — тогда видно настоящую прибавку от модели."""),

    ("""**Ловушка.** Объединение классов 1 и 2 в один даёт событийный F1 = 1.000. Это не
достижение: класс 2 размечен детерминированным правилом (три точки, в середине ноль),
и модель восстанавливает правило, а не учится детекции. Тот же механизм, что в вопросе
со звёздочкой. Если студент приносит такое решение — это повод обсудить утечку,
а не поставить высший балл.""",

     """**Ловушка.** Объединение классов 1 и 2 в один даёт событийный F1 = 1.000. Засчитывать
это как улучшение нельзя по двум причинам. Во-первых, меняется сама задача: эталонных
событий становится 136 вместо 124, и число уже не сравнимо с остальной таблицей.
Во-вторых, в цель затягиваются заправки с выключенным двигателем, а они, как показано
в вопросе со звёздочкой, отделяются одним вычитанием. Хороший повод разобрать со
студентом, почему высокая метрика сама по себе ничего не доказывает."""),
]

slide_pairs = [
    ('''        {"t": "Модель для заправок с выключенным двигателем даёт accuracy 100% и F1 = 1.0",
         "size": 15},''',
     '''        {"t": "11 758 стоянок, из них 711 — заправки. Отличить можно одним "
              "вычитанием: уровень после минус уровень до.", "size": 15},'''),
    ('''        {"t": "Почему это ничего не говорит о качестве детекции?",
         "size": 17, "bold": True, "color": PURPLE, "space": 12},''',
     '''        {"t": "Порог даёт F1 0.968, сеть — 1.000. Так что означает её «стопроцентная точность»?",
         "size": 16, "bold": True, "color": PURPLE, "space": 10},'''),
]

fig_pairs = [
    ('''ax.annotate(f"порог {best[0]:.0f} л: F1 = {best[1]:.3f}", (best[0], ax.get_ylim()[1] * 0.35),
            xytext=(6, 0), textcoords="offset points", fontsize=10)''',
     '''ax.annotate(f"порог {best[0]:.0f} л: F1 = {best[1]:.3f}", (best[0], 120),
            xytext=(8, 0), textcoords="offset points", fontsize=10)'''),
]


def patch(path, pairs, label):
    s = io.open(path, encoding="utf-8").read()
    for old, new in pairs:
        if old not in s:
            sys.exit("[" + label + "] НЕ НАЙДЕНО:" + NL + old[:200])
        s = s.replace(old, new, 1)
    io.open(path, "w", encoding="utf-8").write(s)
    print(label + ": переписано " + str(len(pairs)))


patch(NB, nb_pairs, "ноутбук")
patch(README, readme_pairs, "README")
patch(SLIDES, slide_pairs, "слайды")
patch(FIG, fig_pairs, "график")
